<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ua.art.newsaggregator"
    android:versionCode="1"
    android:versionName="1.0">

    <!-- Internet permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboardHidden|orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.ListNewsView"/>
        <activity android:name=".controller.db.ORMLiteActivity"/>
        <activity android:name=".view.LoginActivity"
        android:theme="@style/Theme.AppCompat.Light.Dialog"
        android:configChanges="keyboardHidden|orientation|screenSize"/>
        <activity android:name=".view.LoginActivityReg"
            android:theme="@style/Theme.AppCompat.Light.Dialog"
            
            android:configChanges="keyboardHidden|orientation|screenSize"/>
        <!--<activity-->
            <!--android:name=".navigation.ButtonAddTable"-->
            <!--android:screenOrientation="portrait"></activity>-->
        <!--<activity android:name=".service_http.ListNewsView"></activity>-->

    </application>

</manifest>
